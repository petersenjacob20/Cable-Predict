# helper.py
# This module is not required for now, but placeholder is here in case we expand.

# Example helper (not used yet):
def format_timestamp(ts_string):
    from datetime import datetime
    return datetime.strptime(ts_string, "%Y-%m-%d %H:%M:%S")
